# Roboto SAI VS Code Extension

## Build (Windows)
1. Node 18+
2. `npm install`
3. `npm run build` → produces .vsix

## Runtime
- Extension spawns `llama-server.exe` on localhost
- Config: `roboto-sai.modelPath`, `roboto-sai.port=8787`
